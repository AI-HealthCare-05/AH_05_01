# SYSTEM_DESIGN — 틈튼(TMTN) 시스템 설계 단일 기준

> 설계 판단이 갈리면 이 문서가 기준입니다.
> 충돌 시 우선순위: **도구 설정**(`pyproject.toml`, ruff/mypy, Gradle) > **이 문서** > `AGENTS.md` > 일반 관례.
> 이 문서와 다르게 가야 할 이유가 생기면 **멈추고 팀에 물어보세요.** 몰래 맞추지 않습니다.

## 1. 시스템 전체 그림

```
[Android 앱]  Kotlin · Compose · com.tmtn.app
     │  /api/v1 (환경별 API 주소)
     ▼
[Nginx] → [FastAPI]  ─┬─→ [MySQL 8.0.46]   회원·기록·챌린지·지수 스냅샷
                      ├─→ [Redis 7.2]       캐시 · Stream
                      ├─→ [또래 브릿지]     Python 3.14.7 / loopback 8766
                      └─→ [개인 SHAP]       Python 3.14.7 / loopback 8776
```

## 2. 모델 세 개를 어디서 돌리나

| 모델 | 위치 | 이유 |
| --- | --- | --- |
| ① 허리둘레 추정 | **서버 추론 API** | 학습된 sklearn 모델. 버전 관리와 교체가 서버에서 쉬움 |
| ② 행동 인식 (걸음·계단·활동시간) | **온디바이스** | 센서 데이터는 기기에 있음. 서버 왕복이 불필요하고 개인정보 측면에서도 낫다 |
| ③ 건강 참고정보·개인 XAI | **서버 API + 별도 모델 프로세스** | 또래 참고정보와 질환별 SHAP를 구분하고, 동의·입력·모델 버전·공개 게이트 확인 |

현재 연결은 Android의 `network/`·`sensor/`·`ui/reference/`·`ui/journal/`, 서버의 `app/services/`, 개인 SHAP의 `model_service/`에 있습니다. 과거 `ModelRegistry.kt` 슬롯 설계는 현재 파일 구조가 아닙니다. `ai_worker/`는 별도 Worker 구성으로, 개인 SHAP 실행 경로와 구분합니다. 동결 원본·실행 환경은 [XAI 안내](docs/XAI.md)를 따릅니다.

## 3. 틈튼지수 — 검증 게이트

이 프로젝트에서 가장 되돌리기 어려운 설계 결정입니다.

- 질환별(당뇨 관련 · 고혈압 관련) **연속형 참고 점수 + 중립적 3단계 + 주요 근거 · 한계**를 표시합니다.
- **종합 공식이 검증되지 않았으면 임시 평균을 만들지 않습니다.** `composite_formula_version` 이 없으면 종합 숫자를 **표시하지 않습니다** — "질환별 결과" 또는 "준비 중"만 보여줍니다.
- 질환 모델은 **공개 게이트를 통과해야** 공개합니다: 누설 점검 · 독립 holdout · 시점 검증 · calibration · 하위집단 성능 · 재현성 · 안전 문구 승인.
- 카드 · 챌린지 · 기록(비버 · 댐) 루프는 이 게이트와 **독립적으로** 검증 가능합니다. 모델이 안 끝나도 행동 루프는 돌아갑니다.

**이건 미완성이 아니라 설계된 안전장치입니다.** 근거가 설 때까지 숫자를 안 보여주는 것이 비진단 서비스에서 옳은 선택입니다.

## 4. 계층과 의존 방향

의존성은 **안쪽으로만** 향합니다. 폴더 이름이 아니라 **import 방향으로** 판단합니다.

```
도메인(순수 로직)  ←  응용(오케스트레이션)  ←  어댑터(DB·HTTP·센서)  ←  조립
```

- 도메인은 프레임워크·I/O 를 import 하지 않습니다. 평범한 단위 테스트로 검증됩니다.
- 경계를 넘을 때는 안쪽이 인터페이스(`Protocol`/ABC)를 소유하고 바깥쪽이 구현합니다. 반대 방향은 없습니다.
- 경계를 넘는 데이터는 단순 구조입니다. **ORM 객체나 DB 행을 안쪽으로 넘기지 않습니다.**

### 과설계 금지 (같은 무게로 지킵니다)

- 통과만 하는 코드로 채워질 계층·패키지·파일을 만들지 마세요.
- 클래스마다 파일 하나로 쪼개지 마세요. 응집된 것은 한 모듈에 둡니다.
- 가상의 미래를 위해 패턴·프레임워크·설정 계층을 넣지 마세요. **두 번째 구체 사례가 생기면** 그때 넣습니다.

## 5. Python 타입

- **Python 3.13** 기준. 최신 문법을 씁니다.
  - 내장 제네릭: `list[int]`, `dict[str, float]` — `typing.List` 금지
  - 유니온: `int | None` — `typing.Optional`, `typing.Union` 금지
  - 네이티브 제네릭·타입 별칭(PEP 695): `def first[T](items: list[T]) -> T`, `type UserId = int`
- `typing` 에서 가져오는 것은 **내장 대체가 없는 이름만** — `Any`, `Protocol`, `Literal`, `TypedDict`, `cast`, `TYPE_CHECKING`.
- 공개 함수·메서드는 매개변수와 반환 타입을 **모두** 표기합니다.

## 6. 스타일은 도구가 강제합니다

산문으로 스타일을 강제하지 않습니다. 커밋 전에 아래를 돌리고, 이걸 통과하면 스타일은 맞는 겁니다.

```bash
uv run ruff format .
uv run ruff check --fix .
```

`pre-commit` 이 설치돼 있으면 커밋 시 자동으로 돕니다.

## 7. 테스트

- 단위·통합 테스트는 **네트워크 0** 입니다. 경계 인터페이스에 가짜를 주입합니다.
- 실제 외부 호출이 필요한 테스트는 `-m slow` 로 분리합니다.
- **아키텍처 규칙(계층 import 방향, 도메인 순수성)도 테스트로 고정합니다.** 문서로만 적어 두면 반드시 깨집니다.
- 동일 입력에 동일 출력이 나오는지 확인하는 재현성 테스트를 유지합니다 (seed 고정 + 반복).

## 8. 개인정보

- 사용자의 건강 수치는 서비스 운영에 필요한 최소 범위만 저장합니다.
- **KNHANES 원본, 참여자 키가 든 CSV, 학습된 모델 파일은 버전 관리에서 제외합니다.**
- 로그·오류 메시지·리포트에 개인 식별 정보가 흐르지 않게 합니다. 마스킹 지점은 **어댑터 출구**입니다.
- 앱에서 받는 값은 **출생연월**까지입니다. 정확한 생년월일을 받지 않습니다.

## 9. 배포

- 초기 APK 목표일은 2026년 9월 14일이었습니다. 현재 릴리스는 README와 [통합 기록](docs/INTEGRATION_2026-09-21.md)을 확인합니다.
- 이미 병합된 코드를 서버에 적용하는 순서는 [EC2 배포](docs/DEPLOYMENT.md)를 따릅니다. Git 병합을 실제 배포 완료로 간주하지 않습니다.
- 운영에서 mysql·redis 포트를 공인망에 노출하지 않습니다.
- 이미지 태그는 고정합니다. `latest` 같은 가변 태그를 쓰지 않습니다.
