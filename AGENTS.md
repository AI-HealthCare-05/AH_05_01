# AGENTS.md — 틈튼(TMTN) 팀 공통 규칙

> 이 파일은 저장소 루트에 있고 **모든 AI 코딩 도구**(Claude Code · Codex · Cursor · Copilot)가 읽습니다.
> 디렉터리별 세부 규칙은 `app/AGENTS.md` · `android/AGENTS.md` · `tmtn_ai/AGENTS.md` 에 있습니다.
> 설계 판단이 필요하면 `SYSTEM_DESIGN.md` 가 단일 기준입니다.

## 이 프로젝트가 무엇인가

만성질환(고혈압·당뇨) 생활습관 챌린지 **안드로이드 앱** `틈튼(TMTN)`.
하루 한 장 카드를 뽑아 작은 행동 하나를 하고, 그 기록이 **댐**으로 쌓입니다.

**목표는 2026년 9월 14일 APK 배포입니다. 해커톤 데모가 아닙니다.**
무엇을 먼저 할지 정할 때 "데모에서 사고 난다"가 아니라 **"배포일에 못 낸다 / 실사용자가 부딪힌다"** 를 기준으로 판단하세요.

| 담당 | 영역 | 코드 위치 |
| --- | --- | --- |
| 문홍주 | 안드로이드 · UX · 팀장 | `android/` |
| 장혁수 | 백엔드 · DB · 인프라 | `app/` `ai_worker/` `infra/` |
| 박강호 | 모델 · 데이터 | `tmtn_ai/` |
| 권병학 | 모델 지원 · 백엔드 지원 · QA | `tmtn_ai/` `tests/` |
| 김지현 | 챌린지 콘텐츠 · 프론트 | `android/app/src/main/assets/missions.json` |

## 출력 언어

**모든 산출물은 한국어로 씁니다** — 주석, docstring, 커밋 메시지, 문서, 사용자에게 보이는 오류 문구, 대화 응답.
영어로 두는 것은 **식별자(변수·함수·클래스·테스트 이름)와 언어 키워드뿐**입니다.

---

# 하지 말 것

이 절이 이 문서에서 가장 중요합니다. 아래는 전부 **실제로 문제를 일으킨 적이 있거나, 일으키면 되돌리기 어려운 것들**입니다.

## 안전·표현

- **"진단" "발병 확률" "당신은 ○○일 가능성이 N%" 같은 표현을 화면·문서·주석에 쓰지 마세요.** 이 앱은 의료기기가 아닙니다. 허용되는 표현은 **비진단용 참고 정보**뿐입니다.
- **비진단 안전 문구를 지우지 마세요.** 화면이 좁다는 이유로도 안 됩니다.
- **검증 게이트를 통과하지 않은 종합 점수를 화면에 내지 마세요.** 종합 틈튼지수는 `composite_formula_version` 이 없으면 **숫자를 그리는 코드 경로 자체를 만들지 않습니다.** 질환별 참고 점수만 표시합니다.
- **행동 완료를 위험도 재계산 트리거로 쓰지 마세요.** 재계산은 건강 입력값이 바뀌거나 모델 버전이 바뀔 때만 합니다.

## 데이터

- **KNHANES 원본 데이터, 참여자 키가 들어간 CSV, 학습된 모델 파일을 커밋하지 마세요.** `tmtn_ai/artifacts/` 는 `.gitignore` 대상입니다.
- **2023 라벨·양성 수·성능 수치를 개발자 경로에 넣지 마세요.** 개발자에게 가는 것은 피처 CSV와 manifest, SHA-256 뿐입니다.
- **`.env`, keystore(`.jks`/`.keystore`), API 키, 서명 비밀번호를 커밋하지 마세요.**
- **APK·빌드 산출물·`.venv`·`__pycache__` 를 커밋하지 마세요.**

## Git

- **`main` 에 직접 커밋하지 마세요.** `main` + `develop` GitFlow 이고 기능 브랜치 접두사는 `feat/` 입니다. 기준 문서는 `docs/GIT_BRANCH_STRATEGY.md`.
- **구조 변경과 동작 변경을 한 커밋에 섞지 마세요.** 둘 다 필요하면 구조 변경을 먼저 하고 테스트로 동작이 그대로인지 확인한 뒤 다음 커밋을 만듭니다.
- **테스트가 깨진 채로 커밋하지 마세요.** 린터 경고도 남기지 않습니다.
- **마이그레이션 파일을 손으로 고치지 마세요.** `uv run aerich migrate` 로 생성합니다.

## 문서

- **실측하지 않은 수치를 문서에 쓰지 마세요.** "약 90% 정확도" 같은 문장은 어디서 나온 숫자인지 함께 적을 수 없으면 쓰지 않습니다.
- **"완료"라고 쓰기 전에 무엇으로 확인했는지 함께 적으세요.** 빌드 통과인지, 실기기 확인인지, 테스트 통과인지 구분합니다.
- **폐기된 결정을 문서에 남겨 두지 마세요.** 지우고, 필요하면 「결정 기록」에 한 줄로 남깁니다.

## 코드 (언어 공통)

- **함수 안에서 import 하지 마세요.** 모든 import 는 파일 맨 위에 둡니다. 순환 참조를 끊기 위한 `TYPE_CHECKING` 블록만 예외입니다.
- **계산된 실제값을 테스트 기대값에 복사하지 마세요.** 기대값은 독립적으로 유도합니다.
- **구현이 두 개 생기기 전에 추상화(베이스 클래스·인터페이스·팩토리)를 만들지 마세요.**
- **통과만 하려고 테스트를 지우거나 약하게 고치지 마세요.**
- **지시받지 않은 리팩터링을 끼워 넣지 마세요.** 눈에 띄면 말로 제안하고, 별도 커밋으로 합니다.

---

# 할 것

## 커밋 전에 반드시

```bash
uv run ruff format .
uv run ruff check --fix .
uv run pytest
```

`pre-commit` 이 설치돼 있으면 앞의 둘은 커밋 때 자동으로 돕니다.

```bash
uv run pre-commit install       # 클론 후 한 번
```

## 커밋 메시지

한국어로 쓰고, **구조 변경인지 동작 변경인지** 접두사로 밝힙니다.

```
structural: TmtnStore 를 data 패키지로 이동
behavioral: 쉼 주 2회 제한을 서버 카운트 기준으로 변경
```

기존 저장소 커밋이 쓰던 이모지 접두사(`✨ feat(android):`)도 그대로 허용합니다. 둘 중 하나로 통일만 하세요.

## 자주 쓰는 명령

```bash
uv run pytest                                   # 전체 테스트
uv run pytest path/to/test_x.py::test_name      # 하나만
uv run ruff format . && uv run ruff check --fix .
uv run mypy .                                   # 타입 검사
docker compose up -d mysql redis                # 로컬 DB·캐시만 띄우기
uv run aerich migrate --name <설명>              # 마이그레이션 생성
uv run aerich upgrade                           # 마이그레이션 적용
```

`git` 을 원격 도구로 실행할 때는 `--no-optional-locks` 를 붙이세요.

## 모르면 물어보세요

아래는 **혼자 정하지 말고 사람에게 확인**해야 합니다.

- 예측 타깃·평가 지표·공개 게이트 기준을 바꾸는 일
- 화면에 나가는 안전 문구·의학적 표현
- DB 스키마의 파괴적 변경(컬럼 삭제, 타입 변경)
- 팀 공통 버전(`docs/DEVELOPMENT_ENVIRONMENT.md`)을 올리는 일
- 새 라이브러리·프레임워크 추가

---

# 팀 공통 버전

## ★ 버전은 `docs/DEVELOPMENT_ENVIRONMENT.md` 가 단일 기준입니다

버전을 물어보거나 쓰기 전에 **그 문서를 여세요.** 이 파일에 표를 두 벌로 두면 반드시 어긋납니다.
아래는 **자주 틀리는 것만** 옮겨 둔 것이고, 값이 다르면 언제나 `DEVELOPMENT_ENVIRONMENT.md` 가 맞습니다.

그 문서가 정한 우선순위:

1. CI·빌드 설정 (`.github/workflows/`, Gradle wrapper, Dockerfile)
2. 잠금·버전 파일 (`uv.lock`, `.python-version`, `libs.versions.toml`, Compose image tag)
3. 프로젝트 제약 (`pyproject.toml`, Gradle build script)
4. 문서와 README

**불일치를 발견하면 설정에 몰래 맞추지 말고, 문서와 설정을 같은 PR 에서 함께 고치세요.**

## 자주 틀리는 값

| 항목 | 값 | 자주 나는 실수 |
| --- | --- | --- |
| Python | `3.13.15` | **3.14 금지** — `asyncmy` 비호환 |
| uv | `0.12.5` | 다른 버전이면 uv 가 실행을 중단합니다 |
| ruff | `0.14.14` | **pre-commit 훅 rev 도 같은 버전이어야 합니다** |
| mypy | `1.19.1` | |
| MySQL / Redis / Nginx | `8.0.46` / `7.2.15-alpine` / `1.28.3-alpine` | |
| AGP / Kotlin / Gradle | `9.1.1` / `2.4.10` / `9.3.1` | |
| **`compileSdk`** | **`37`** | **36 이 아닙니다.** Compose BOM `2026.08.00`(Compose 1.12.0)이 37 이상을 요구해 2026-08-27 에 올렸습니다 |
| `targetSdk` / `minSdk` / JDK | `36` / `28` / `17` | `targetSdk` 는 Play 기준이라 36 유지 — `compileSdk` 와 **다른 게 정상**입니다 |
| Compose BOM | `2026.08.00` | Firebase 와 Compose 는 **BOM 단위로만** 올립니다 |

`latest`, `8.0`, `3.13` 처럼 실행할 때 내용이 달라지는 가변 태그를 새로 쓰지 마세요.

## 버전을 올려야 할 때

- **기능 브랜치에서 올리지 마세요.** `chore/deps-<날짜>` 같은 전용 브랜치에서만 합니다.
- Python 을 올리면 `.python-version` · `pyproject.toml` · Dockerfile · CI 를 **함께** 고칩니다.
- Android 를 올리면 `libs.versions.toml` · Gradle wrapper · AGP·Kotlin · 문서를 **함께** 고칩니다.
- **release 브랜치에서는 보안 hotfix 외의 도구체인 업그레이드를 하지 않습니다.**
- alpha·beta·RC 라이브러리는 ADR 과 팀 승인 없이 쓰지 않습니다. (예: `Navigation 2.10.0-rc` 사용 금지)

## 버전 표기 규칙

| 항목 | 규칙 |
| --- | --- |
| 앱 버전 | Semantic Versioning `MAJOR.MINOR.PATCH`, 최초 `0.1.0` |
| Git tag | `v0.1.0` 형식 |
| Docker 이미지 | API `app-0.1.0` · Worker `ai-0.1.0` |
| Android `versionName` | 릴리스 SemVer 와 일치 |
| Android `versionCode` | 배포마다 증가하는 정수. **절대 재사용 금지** |
| API | `/v1` 계약. breaking change 는 `/v2` |
| 모델 | model version · feature version 을 함께 기록 |

**백엔드와 앱이 항상 동시에 업데이트된다고 가정하지 마세요.** `/v1` 안에서는 구버전 앱의 계약을 깨지 않습니다.

---

# 도메인 규칙 — 코드로 옮길 때 틀리기 쉬운 것

## 쉼

- 사용자가 **직접** "오늘은 쉬어가기"를 눌러야 쉼입니다. **자동으로 쉼이 되지 않습니다.**
- 쉼으로 표시한 날은 **연속 기록이 끊기지 않습니다.**
- 한 주에 **두 번까지**. 이 카운트는 **서버에서** 셉니다 (기기를 바꿔도 맞아야 하므로).
- 아무것도 안 하고 지나간 날은 **미완료**이고, 이때는 연속 기록이 끊깁니다.

## 재료 5종

나뭇가지(움직임·유산소) · 받침돌(근력) · 물길(수분) · 다짐흙(생활리듬) · 새잎(식사·기록)

## 미션 분류

미션 200장 중 **운동 미션은 80장**(유산소 40 + 근력 40)입니다. 나머지 120장(식사·수면·생활리듬 / 기록 / 수분·회복)은 운동이 아니므로 **모델 입력에 넣지 마세요.**

운동 강도는 「한국인을 위한 신체활동 지침서」(2023) 기준을 따릅니다 — 저 1.5~3.0 MET / 중 3.0~6.0 MET / 고 6.0 MET 이상.

**중강도 환산식**: `중강도 분 + 2 × 고강도 분`. **저강도는 이 식에 들어가지 않습니다.**

## 말투

비버가 사용자에게 말할 때는 **반말·짧게** ("잘했어. 내일도 이만큼이면 충분해.").
시스템 안내는 **존댓말**. **실패를 사용자 탓으로 돌리지 않습니다.**
"마이" 라는 말은 쓰지 않습니다 — **"내 정보"** 입니다.

---

# 결정 기록

되돌리지 말아야 할 결정만 한 줄씩. 자세한 배경은 `SYSTEM_DESIGN.md` 와 `docs/` 에 있습니다.

| 날짜 | 결정 |
| --- | --- |
| 2026-08-17 | APK 배포가 제출물로 승인됨. Play 스토어는 포기하고 서명 APK 직접 배포 |
| 2026-08-19 | 플랫폼을 웹(React)에서 **Android 네이티브 Compose** 로 재전환 |
| 2026-08-14 | **리듬 타입 개념 폐기.** 사용자를 타입으로 분류하지 않습니다 |
| 2026-08-26 | **"100명 중 30명" 코호트 문장 폐기.** 틈튼지수(질환별 참고 점수 + 검증 게이트)로 대체 |
| 2026-08-27 | 초록 팔레트(`#0C3B2E` `#6D9773`) **폐기**. 순백 + 먹색으로 교체 |
| 2026-08-27 | GitFlow `main` + `develop`, 기능 브랜치 접두사 `feat/` 로 확정 |
| 2026-08-31 | **9/14 APK 배포가 목표**임을 명확히 함. 해커톤 데모 아님 |
| 2026-08-31 | 모델 배치 확정 — 허리둘레·틈튼지수는 **서버 추론**, 행동인식은 **온디바이스** |
