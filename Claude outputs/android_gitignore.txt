*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
.kotlin/

# ⚠️ 2026-09-10 추가: Gradle 홈(GRADLE_USER_HOME)이 이 폴더 안에 잡혀 있어서
# 아래 7개가 프로젝트 안에 생김. 전부 캐시·다운로드본이라 커밋 대상이 아니고,
# 합쳐서 약 1.85GB임(이것 때문에 폴더 압축이 안 됐음).
# ⚠️ 이 항목들이 .gitignore에 없으면 `git add -A` 한 번에 1.85GB가 스테이징됨 - 반드시 먼저 넣을 것.
# 참고: /wrapper 는 Gradle 홈의 배포본 캐시이고, 커밋해야 하는 래퍼는 gradle/wrapper/ 라 서로 다름.
/caches/
/daemon/
/jdks/
/native/
/kotlin-profile/
/tools/
/wrapper/

# Android Studio가 가끔 만드는 빈 폴더(FakeDependency.jar). 지워도 다시 생길 수 있어 무시함.
/android/
