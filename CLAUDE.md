# CLAUDE.md

> **먼저 저장소 루트의 `AGENTS.md` 를 읽으세요. 프로젝트 규칙의 본체는 거기 있습니다.**
> 이 파일에는 Claude Code 에만 해당하는 것만 둡니다. 규칙을 여기 복사하지 마세요 —
> 두 벌이 되면 반드시 어긋나고, 어긋난 순간부터 둘 다 못 믿게 됩니다.

## 읽는 순서

1. `AGENTS.md` — 팀 공통 규칙, 금지 사항, 버전, 도메인 규칙
2. `SYSTEM_DESIGN.md` — 설계 단일 기준. `AGENTS.md` 와 충돌하면 **멈추고 물어보세요**
3. 작업할 디렉터리의 `AGENTS.md` — `app/` · `android/` · `tmtn_ai/`

## 작업 시작 전

- **어느 디렉터리에서 작업하는지 먼저 확인하세요.** 이 저장소에는 성격이 다른 코드가 세 덩어리 있습니다
  (`app/` FastAPI · `android/` Kotlin · `tmtn_ai/` 모델). 남의 영역 규칙을 적용하지 마세요.
- 여러 파일을 훑어야 하는 탐색은 서브에이전트(Explore)에 맡기고 결론만 받으세요.
  이 저장소는 Kotlin 47개 + Python 45개 + `tmtn_ai/` 185개라 전부 읽으면 문맥이 금방 찹니다.

## 작업 끝나고

- 커밋 전 `uv run ruff format . && uv run ruff check --fix . && uv run pytest`
- **`pre-commit` 이 막으면 `--no-verify` 로 넘기지 마세요.** 고쳐진 파일을 `git add` 하고 다시 커밋합니다.

## 이 세션에서 하지 말 것

- **저장소에 없는 파일을 있다고 말하지 마세요.** 확인하고 답합니다.
- **테스트를 돌리지 않고 "통과합니다" 라고 쓰지 마세요.**
- **실측하지 않은 수치를 문서에 넣지 마세요.**
