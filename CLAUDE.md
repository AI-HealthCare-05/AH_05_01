# 틈튼 TMTN — 안드로이드 앱

> 이 파일은 저장소 루트(`AH_05_01/CLAUDE.md`)에 있습니다.
> Claude Code가 세션마다 자동으로 읽습니다. 안드로이드 코드는 `android/` 아래에 있습니다.

## 이게 뭔가

만성질환(고혈압·당뇨) 생활습관 챌린지 앱.

> **목표는 2026년 9월 14일 APK 배포입니다. 해커톤 데모가 아닙니다.**
> 이 차이가 우선순위를 뒤집습니다 — 데모라면 "20분짜리 미션을 배속으로 건너뛰기"가 급하지만,
> 실배포에서 20분은 **정상 동작**이고 대신 **서명 키 · release 빌드 검증 ·
> 화면을 벗어나도 타이머가 흐르는지 · 임시 모델을 실사용자에게 낼지**가 급합니다.
> 무엇을 먼저 할지 정할 때는 "데모에서 사고 난다" 가 아니라
> **"배포일에 못 낸다 / 실사용자가 부딪힌다"** 를 기준으로 보세요.
하루 한 장 카드를 뽑아 작은 행동 하나를 하고, 그 기록이 **댐**으로 쌓입니다.

- 담당: 문홍주 (안드로이드) · 장혁수 (백엔드) · 박강호 (모델) · 김지현 (콘텐츠·캐릭터)
- 디자인 원본: Figma `ByGT2uoinUBxAQOBqAK7sM` · 페이지 `★ 12 · 화면 전체 (프로토타입)` · **107화면**
- 화면 목록과 Figma 바로가기: `docs/design-v5/SCREENS.csv`

## 기술

Kotlin · Jetpack Compose · Material3 · 패키지 `kr.tmtn.app`

## 디자인 시스템 — 어기면 안 되는 것

토큰 파일: `designsystem/TmtnColor.kt` · `TmtnType.kt` · `TmtnDimens.kt`
색은 `TmtnColor.*`, 글자는 `TmtnText.*` 로만 씁니다. 하드코딩 금지.

### 색
- **주황 `#FF7A1A` 은 "오늘"에만 씁니다.** 완료·실천·주 버튼은 전부 먹색 `#16181C`.
  (주황이 많아 눈이 아프다는 피드백으로 정리한 규칙입니다. 깨면 화면이 다시 시끄러워집니다.)
- 화면 바탕 `#FFFFFF` · 카드 바탕 `#F7F4EE`
- 2026-08-27 이전의 초록 팔레트(`#0C3B2E` `#6D9773`)는 **폐기**. 코드에 남아 있으면 지우세요.
- 하드코딩 금지. 반드시 `TmtnColor.*` 를 통해 씁니다.

### 글자
7단계뿐입니다 — Display 40 / Headline 32 / Title 24 / BodyLarge 19 / Body 16 / Label 14 Bold / Caption 14 Medium.
- **Display 는 화면당 하나만.**
- Noto Sans KR 에 SemiBold(600)가 없어 Bold(700)로 대체했습니다.
- 숫자 나오는 곳은 `fontFeatureSettings = "tnum"`.

### 간격·모서리
`TmtnSpace` / `TmtnRadius` / `TmtnTarget` 사용. 누를 수 있는 것은 최소 48dp.

### 화면 높이 예산
390×844 기준으로 상태바 44 + 앱바 64 + 하단탭 104 를 빼면 **본문 632dp** 입니다.
스크롤 없는 화면은 이 안에 들어와야 합니다.

## 지금 코드 상태 (2026-08-30 확인)

- Kotlin 40개 파일. `ui/{auth,card,home,mission,nav,onboarding,tabs}` · `domain/ml` · `data`
- `designsystem/` : `TmtnColor.kt` `TmtnType.kt` `TmtnDimens.kt` `TmtnComponents.kt` `TmtnIcons.kt` `TmtnShape.kt` `TmtnTheme.kt`
- `TmtnComponents.kt` 에 **`ImageSlot(tag, title, desc, height)` 이미 있음** — 일러스트는 이걸로 자리만 잡고 나중에 넣습니다
- `assets/missions.json` 있음

### v4 → v5 토큰 교체 (2026-08-30 완료)
`docs/design-v5/designsystem/` 의 3개 파일이 앱에 반영돼 있습니다. 바이트 단위로 동일합니다.
**객체·프로퍼티 이름을 v4 그대로 맞춰 뒀기에 화면 코드는 고치지 않았습니다.** 값만 바뀌었습니다.

| 파일 | 무엇이 바뀌었나 |
|---|---|
| `TmtnColor.kt` | 초록 → 순백+먹색. 이름 동일 + 재료 5색·`Shadow`·`OnSurfaceFaint` 추가 |
| `TmtnType.kt` | 크기 상향 (Display 32→40, Title 18→24, Body 15→16, Caption 12→14). `TmtnText`·`Metric`·`TmtnTypography` 이름 그대로 |
| `TmtnDimens.kt` | **신규.** 간격·모서리·달력 규격·화면 예산 |

검증한 것 — `:app:compileDebugKotlin` 강제 재컴파일 통과(오류·경고 0), `app-debug.apk` 생성,
화면이 쓰는 `TmtnColor.*` 21개·`TmtnText.*` 8개 전부 존재, 폐기된 초록 잔존 없음, 하드코딩 색 없음.

> **아직 남은 확인**: 글자가 커져서 기존 화면 일부가 넘칠 수 있습니다. 이건 빌드로는 안 잡힙니다.
> 실제 화면 파일은 모두 `verticalScroll`/`Lazy*` 를 쓰고 있어 위험은 낮지만,
> 에뮬레이터로 하나씩 돌려 보고 넘치는 곳만 여백을 줄이세요. 글자를 다시 줄이지는 마세요.

## 앱 구조

하단 탭 **5개**: 홈 · 기록 · **틈튼지수** · 댐 · 내 정보
- 틈튼지수가 이 앱의 핵심이라 자기 탭을 가집니다 (`E01`).
- 카드첩은 댐 탭 안에 있습니다 (`G06`).
- "마이" 라는 말은 쓰지 않습니다. **"내 정보"** 입니다.

## 도메인 규칙 — 코드로 옮길 때 주의

### 쉼 (핵심 장치)
- 사용자가 **직접** "오늘은 쉬어가기"를 눌러야 쉼으로 기록됩니다. 자동으로 쉼이 되지 않습니다.
- **쉼으로 표시한 날은 연속 기록이 끊기지 않습니다.**
- 한 주에 **두 번까지**. 이 카운트는 **서버에서** 세야 기기를 바꿔도 맞습니다.
- 아무것도 안 하고 지나간 날은 **미완료**이고, 이때는 연속 기록이 끊깁니다.
- 쉼 버튼 위치: `B01` `B01b` `B06` `C01`

### 기록 달력의 날짜 4상태
| 상태 | 표시 |
|---|---|
| 실천 | 먹색 꽉 찬 원 + 흰 숫자 |
| 쉼 | 먹색 **실선** 테두리 1.5dp + 바탕 `#EFEAE0` |
| 미완료 | 회색 `#B8B2A6` **점선** 테두리 1.5dp (`dash 2.5 / gap 2.5`) |
| 오늘 | 주황 실선 테두리 2dp |

**실선 = 쉼(내가 고른 것), 점선 = 미완료(빠진 것).** 색만이 아니라 선 종류로 구분합니다. 이 대비를 없애지 마세요.

주간·월간 달력 모두 **월요일 시작**입니다.

### 재료 5종
나뭇가지(움직임·유산소) · 받침돌(근력) · 물길(수분) · 다짐흙(생활리듬) · 새잎(식사·기록)

### 안전 문구
의료기기가 아닙니다. 틈튼지수는 **비진단용 참고 정보**로만 표시합니다. 이 문구를 빼지 마세요.

## 말투

비버가 사용자에게 말할 때는 반말·짧게 ("잘했어. 내일도 이만큼이면 충분해.").
시스템 안내는 존댓말. 실패를 사용자 탓으로 돌리지 않습니다.

## 아직 없는 것

- 비버 일러스트 8포즈 · 댐 단계 5장 → `docs/design-v5/ASSETS_필요한이미지.md`
- 로고 심볼 (앱 아이콘 샘플은 `docs/design-v5/brand/tmtn_app_icon_512.png` 로 임시 확정)
- 운세 200장 CSV는 **김지현 님**이 수정 예정
- 모델 3종(허리둘레·행동인식·틈튼지수)은 임시 구현. `domain/ml/ModelRegistry.kt` 한 파일에서 교체

## 작업할 때

- 이미지는 나중에 넣습니다. 자리는 `ImageSlot` 하나로 통일하고 파일 이름만 미리 정해 두세요.
- 화면을 새로 만들기 전에 `docs/design-v5/SCREENS.csv` 에서 그 화면의 Figma 링크를 열어 실제 디자인을 확인하세요.
